<div class="layout-content">
<div class="align-center">
      <img src="<%= @user.photo %>" width="300px" >
      <h1 class="light"> <%= @user.name %> </h1>
      <p class="light"><%= @user.speciality %></p>
      <p class="light"><%= @user.description %></p>

     <%= link_to "Book A Session", url_for(controller: "bookings", action: "new", user_id: @user.id), class: "btn-gf" %>




</div>
</div>